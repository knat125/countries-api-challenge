const BASE = "https://restcountries.com/v3.1";

const ALL_COUNTRIES = `${BASE}/all`;
const COUNTRY_BY_NAME = `${BASE}/name`;
const COUNTRY_BY_CODE = `${BASE}/alpha`;
const COUNTRIES_BY_CONTINENT = `${BASE}/continent`;

export { ALL_COUNTRIES, COUNTRY_BY_NAME, COUNTRY_BY_CODE, COUNTRIES_BY_CONTINENT };